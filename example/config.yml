info:
  title: Best Fetstore

update:
  ignore:
    folders:
      - /pet/findByTags
      - /api/admin
    ids:
      - addPet
      - uploadFile

auth:
  type: bearer
  value:
    token: "{{accessToken}}"
  ignore:
    ids:
      - login
    folders:
      - /user
